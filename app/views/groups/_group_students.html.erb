<br><br>
<h1>Students</h1>
<table class='table table-stripped table-hover'>
  <thead>
    <tr>
      <th>Age</th>
      <th>Firstname</th>
      <th>Member since</th>
      <th>Comment</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% group.users.each do |user| %>
      <tr>
        <td><%= user.age %></td>
        <td><%= user.firstname %></td>
        <td><%= user.firstname %></td>
        <td><%= menber_since(user.id, group.id) %></td>
        <td><%= user.comment %></td>
        <td><%= link_to 'Show', user %></td>
      </tr>
    <% end %>
  </tbody>

</table>